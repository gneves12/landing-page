*{margin: 0; padding: 0;}
html{
    scroll-behavior: smooth;
}

body{
    font-family: arial, helvetica, sans-serif;
    font-size: 20px;
   
}

#header{
    position:fixed;
    margin: 0px;
    width: 100%;
    background-color: white;
}

#menu1{
    margin-right: 50px;
}

#logo{
    margin-left: 90px;
}

.menu{
    list-style:none;
    margin-top: 25px;
    top: 0%;
    left: 22%;
    position: fixed;
    width: 100%;
    background-color: white;
}
.menu li{
    position:relative;
    float:left;
    font-family:system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
}

.menu li a{color:orange; text-decoration:none; padding:10px 10px; display:block;}

.menu li a:hover{
    background:orange;
    color:#fff;
    text-shadow:0px 0px 5px #fff;
}

.menu li ul{
    position:absolute;
    top:45px;
    left:0;
    background-color:#fff;
}

.menu li ul li{
    display:block;
    width:150px;
    background-color:whitesmoke;
    font-size: 17px;
}
